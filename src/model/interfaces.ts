export interface IEntityDescription {
  id: string;
  description: string;
}

export interface IPlayer {
  id: string;
  name: string;
  surname: string;
  nickname: string;
  lastSeasonPosition: number;
}
